import React from 'react';
import ReactDOM from 'react-dom';
import 'antd-mobile/dist/antd-mobile'; //导入antd-mobilede样式
import './index.css'; //导入全局通用样式(也可按需导入)
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
